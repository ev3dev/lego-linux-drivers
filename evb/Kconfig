menuconfig FATCATLAB_EVB
    tristate "FatcatLab EVB support"
    help
      Say Y here if you want to use FatcatLab's EVB.

      To compile this driver as a module, choose M here: the
      module will be called evb.
